# @agent/shared

## 职责

- 存放跨包复用的共享类型、协议模型和最基础的接口定义。
- 只表达数据结构和抽象契约，不承载运行时业务逻辑。

## 允许依赖

- 第三方类型依赖，例如 `openai` 的类型定义。
- 与类型声明直接相关、不会引入业务行为的轻量依赖。

## 禁止依赖

- `@agent/memory`
- `@agent/tools`
- `@agent/mcp`
- `@agent/core`
- `@agent/server`
- 任何数据库、网络、子进程、文件系统等运行时实现逻辑

## 对外入口

- `src/index.ts`

