{
  "name": "@agent/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --disable-warning=ExperimentalWarning --experimental-transform-types src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --disable-warning=ExperimentalWarning --experimental-transform-types --test test/**/*.test.ts"
  },
  "dependencies": {
    "@agent/core": "workspace:*",
    "@agent/shared": "workspace:*",
    "@agent/workflow": "workspace:*",
    "dotenv": "^17.4.2",
    "koa": "^2.16.2"
  },
  "devDependencies": {
    "@types/koa": "^2.15.0"
  }
}
